# @bloxplug/loader

```ts
import { Loader } from "@bloxplug/loader"
import { ReplicatedStorage } from "@rbxts/services"

Loader.load({
	paths: ["lobby"],
	useLua: false // For debug purposes
}) // Loads all scripts under folders 'lobby' named 
```